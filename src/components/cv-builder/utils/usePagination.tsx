import React, { useState, useEffect, useRef } from 'react';

export function usePagination(items: any[]) {
  // We won't actually use a complex generic hook. 
  // It's easier to build it directly in the template.
}
